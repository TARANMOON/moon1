# Browser Support

Chrome
Safari
Firefox
Edge
iOS Safari
Android Chrome

## Fallback

If cinematic rendering fails:
content must remain accessible.

Visual enhancement must never become a functional dependency.
